// First and last item of the array

arr = [1, 2, 3, 4, 2]
last = arr.length - 1

console.log(arr[0])
console.log(arr[last])  

// Minimum and Maximum of the array

let min = Math.min( ...arr )
let max = Math.max( ...arr )

console.log(min)
console.log(max)